// src/setupTests.js
// Global setup for all Vitest tests.
// Imports jest-dom matchers so you can write:
//   expect(element).toBeInTheDocument()
//   expect(element).toHaveTextContent('...')
//   expect(element).toHaveClass('...')
import '@testing-library/jest-dom';
